import 'package:flutter/material.dart';

/* style configs */
const kDefaultRadius = 5.0;
const kDefaultTransparency = 0.2;
const kDefaultPadding = 12.0;

const kDefaultSearchBarSize = 50.0;

/* behavior */
final kDefaultShadow = BoxShadow(
  offset: Offset(0, 10),
  blurRadius: 50,
  color: Colors.grey.withOpacity(kDefaultTransparency),
);
